# Storia: prima si usava Git LFS per /public/foto e /public/video, ma la quota
# GitHub LFS si era esaurita e Vercel materializzava solo i pointer (130 byte di
# testo) → immagini vuote sul sito live. Ora gli asset in /public sono blob git
# normali; sono comunque pre-ottimizzati a max 1920px / JPEG q78 da
# scripts/optimize-images.mjs (~170 KB di media).
